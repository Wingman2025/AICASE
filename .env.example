# OpenAI API Key (required for the agents)
OPENAI_API_KEY=your_openai_api_key_here

# Database configuration (will be set automatically by Railway if using their PostgreSQL)
# DATABASE_URL=postgresql://username:password@hostname:port/database_name

# Port configuration (Railway will set this automatically)
# PORT=8050

# Railway specific configuration
# RAILWAY_STATIC_URL=/static/
